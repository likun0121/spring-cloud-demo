spring:
  application:
    name: product
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: dev
  rabbitmq:
    host: 192.168.56.101
    port: 5672
    username: guest
    password: guest
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/